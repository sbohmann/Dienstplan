extends layout

block scripts
    script(src="/javascripts/index.js")

block content
    div(id='content')
        div#title
            h1=title
            a(href="/menu")
                img(src="/images/gear-icon.png" width=32 height=32)
        div(id='date-navigation')
            span
                a(href=previousYearLink) <<
            span
                a(href=previousMonthLink) <
            span
                a(href=nextMonthLink) >
            span
                a(href=nextYearLink) >>
        div#subtitle
            h2=user_name
            a(href='/pdf/months/' + month.year + '/' + month.month) PDF
        table(id='monats-dienstplan')
            tr
                th(id='week-day-column')
                th(id='day-column')
                th(class='name-column') Name
                th(class='name-column') Reserve
                th(class='name-column') 2. Reserve
            for row in rows
                if row.week_change
                    tr
                        td(colspan=5 class='new-week')
                else
                    tr
                        td=row.weekday
                        td!=row.day_of_month + '.'
                        td(
                            id='primary_' + row.day_of_month
                            class='day_cell'
                        )
                        td(
                            id='reserve_' + row.day_of_month
                            class='day_cell'
                        )
                        td(
                            id='secondReserve_' + row.day_of_month
                            class='day_cell'
                        )
    div(id='selectionDialog' class='dialog')
        div(class='dialogContent')
            p(id='userSelectionText' class='dialogTitle')
            div(id="selectionButtons" class='dialogButtons')
                button(id='cancelUserSelectionButton' class='dialogButton')="Zurück"
    div(id='confirmationDialog' class='dialog')
        div(class='dialogContent')
            p(id='bookingConfirmationText' class='dialogTitle')
            div(class='dialogButtons')
                button(id='cancelBooking' class='dialogButton')="Zurück"
                button(id='confirmBooking' class='dialogButton')="Ja"
